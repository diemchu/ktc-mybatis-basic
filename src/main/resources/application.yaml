
#데이터 베이스 연동하는 부분
spring:
  datasource:
    url: jdbc:sqlite:db.sqlite
    driver-class-name: org.sqlite.JDBC

mybatis:
  mapper-locations: classpath:mybatis/mappers/*.xml
  type-aliases-package: com.example.crud_mybatis.model
  configuration:
    map-underscore-to-camel-case: true